# The upload endpoint is now configured at runtime via the app's settings screen.
# Open the app from your launcher and enter the URL there.
# This file is kept for reference only and is no longer read at runtime.
